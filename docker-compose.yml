services:
  portalite:
    build: .
    ports:
      - "8080:8080"
    restart: unless-stopped
    volumes:
      - ./sample-data:/app/sample-data
      - portalite-config:/app/config
volumes:
  portalite-config:
